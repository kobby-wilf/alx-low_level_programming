#include "main.h"
/**
 * print_alphabet_x10 - functions to print abc 10 times
 *
 * Return: 0 
 */
void print_alphatbet_x10(voi)
{
	char c, i;

	for (i = 0; i <= 9; i++)
	{
		for (c = 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
		_putchar('\n);
	}
}
